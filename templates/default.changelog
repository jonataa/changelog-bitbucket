
<?php foreach ($versions as $version): ?>

  <h1><?= $version['name'] ?> (<?= $version['releaseDate']->format('d/m/Y') ?>)</h1>
  <?php foreach ($version['issuesByType'] as $issuesByType): ?>
          <?php foreach ($issuesByType as $issue): ?>
            <h2><?= $issue['fields']['issuetype']['name'] ?></h2>    
          <?php endforeach ?>
  <?php endforeach ?>

<?php endforeach ?>